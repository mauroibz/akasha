# Akasha v1 has no authentication. Keep it on a trusted LAN.
BOOK_TRACKER_DATA_DIR=/data
BOOK_TRACKER_CALIBRE_DIR=/calibre
# Optional override; defaults to sqlite:////data/books.db when omitted.
# BOOK_TRACKER_DATABASE_URL=sqlite:////data/books.db
GOOGLE_BOOKS_API_KEY=
USER_AGENT_CONTACT=you@example.com
TZ=UTC
LOG_LEVEL=INFO
BOOK_TRACKER_ENVIRONMENT=development
BOOK_TRACKER_SQLITE_BUSY_TIMEOUT_MS=5000

# Backups are written outside the data volume on purpose (DEC-040). Point this
# at a NAS share if you have one, and `chown 10001:10001` it so the container's
# non-root user can write there.
BACKUP_DIR=./backups
BACKUP_RETENTION=7
# Your Calibre library, mounted read-only. Create the directory even if empty.
CALIBRE_DIR=./calibre
# 0.0.0.0 publishes on every LAN interface; 127.0.0.1 keeps it on this machine.
AKASHA_BIND=0.0.0.0
AKASHA_PORT=8000
